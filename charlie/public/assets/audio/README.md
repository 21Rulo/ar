# Audio

Coloca aquí el archivo final con el nombre `audio_placeholder.mp3`
(o cambia la ruta en `AUDIO.background`, src/js/assets.js, y en el
`<audio id="bg-audio">` de index.html si le cambias el nombre).

Hasta que exista el archivo real, el `<audio>` simplemente no reproduce nada
(el error se atrapa en `main.js` y no rompe el flujo).
